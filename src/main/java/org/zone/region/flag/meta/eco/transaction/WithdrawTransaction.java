package org.zone.region.flag.meta.eco.transaction;

/**
 * Transaction to use for all withdraws
 */
public class WithdrawTransaction extends AbstractTransaction {

    public WithdrawTransaction(TransactionBuilder builder) {
        super(builder);
    }
}
